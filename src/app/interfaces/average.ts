export interface AverageOfYear {
  year: number;
  date: string;
  averageOfYear: number;
  category: string;
}
export interface AverageOfMonth {
  year: number;
  month: number;
  date: string;
  averageOfMonth: number;
  category: string;
}
export interface AverageOfWeek {
  year: number;
  week: number;
  date: string;
  averageOfWeek: number;
  category: string;
}
