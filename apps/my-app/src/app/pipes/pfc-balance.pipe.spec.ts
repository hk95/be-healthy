import { PfcBalancePipe } from './pfc-balance.pipe';

describe('PfcBalancePipe', () => {
  it('create an instance', () => {
    const pipe = new PfcBalancePipe();
    expect(pipe).toBeTruthy();
  });
});
